# Copy to .env and fill in. .env is gitignored; never commit it.
# The CLI loads this file into the environment at startup; the code
# itself only ever reads environment variables.
ANTHROPIC_API_KEY=
# Optional: provider selection (anthropic is the default)
# BP2UIP_PROVIDER=anthropic
# OPENAI_API_KEY=
