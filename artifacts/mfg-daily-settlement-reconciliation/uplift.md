# Uplift analysis: MFG - Daily Settlement Reconciliation

Derived view of `uplift.json` in this directory; the JSON is
canonical. Regenerated by the pipeline, do not edit by hand.
Findings cite rule IDs in `docs/uplift-criteria.md`; the analyzer
is pure code over the estate model and the spec, no LLM involved.

- Spec: `spec-86b65907-a49e-4383-b964-06d2d5f22151` (**approved** at analysis time)
- Criteria version: 0.1.0
- Analyzed: 2026-07-21T01:09:17.058585+00:00
- Findings: 1 agentic candidates, 2 human gates, 3 keep deterministic

## Agentic candidates

**uf-03** (AC-3, P-2)

Decision 'Variance Within Tolerance?' routes work to a human gate ('Create Variance Case') on a single configured threshold ([Variance] <= [Tolerance] AND [Variance] >= 0 - [Tolerance]; Tolerance is configuration no stage writes). The threshold is a deterministic proxy for a risk judgment. Candidate: agent-assisted triage that may narrow, never widen, what is auto-processed; the gate itself stays (P-2 keeps the gate stages HUMAN_GATE).

Stages: Variance Within Tolerance? (decision)

## Human gates

**uf-01** (HG-1)

The reviewed intent spec identifies a human touchpoint here: "A case is created in the Case Manager system for settlement variances outside tolerance, implying a human reviewer will investigate and resolve the discrepancy." The handoff stages ('Create Variance Case') are the boundary; migration preserves it.

Stages: Create Variance Case (action)

**uf-02** (HG-2)

'Throw Variance Exception': each ends automated handling and leaves the item in a state a person must investigate and resolve.

Stages: Throw Variance Exception (exception)

## Keep deterministic

**uf-04** (KD-1)

'Accumulate Total', 'Count Rows', 'Compute Variance': deterministic arithmetic and data staging; regulated amounts must not be model-mediated.

Stages: Accumulate Total (calculation); Count Rows (calculation); Compute Variance (calculation)

**uf-05** (KD-2)

'Get Settlement Report': invocations of defined interfaces; the target system's contract does not loosen because a model is upstream.

Stages: Get Settlement Report (action)

**uf-06** (KD-4)

'For Each Row', 'For Each Row': explicit flow control; the condition is fully stated in the source, there is no judgment to uplift.

Stages: For Each Row (loop_end); For Each Row (loop_start)
