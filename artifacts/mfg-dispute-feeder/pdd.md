# Process Definition Document (as-is): MFG - Dispute Feeder

Generated by BP2UIP; do not edit by hand. Every statement in this
document derives from the parsed source, the reviewed intent spec,
or the uplift analysis, and the provenance log next to it records
how it was produced.

- Spec: `spec-21af9fee-0485-43b2-bde7-9a07a1ee017b` (**approved**)
- Approved by: **Chris Williams** at 2026-07-20T20:14:12.670259+00:00
- Extracted by: anthropic/claude-sonnet-5 (prompt v0.1.0)
- Source estate: `artifacts/estate/estate.json` (sha256 `040a1a59dd7e...`)

## 1. Purpose

This process feeds a batch of dispute transactions into the 'Q-Disputes' work queue so that they can be worked downstream (e.g., by a case-handling or investigation process). It iterates over a collection of disputes, stages each dispute's key fields, and loads each one into the queue, tracking how many items were successfully loaded.

Evidence: For Each Dispute (loop_start); Add Dispute To Queue (queue_write); Count Loaded (calculation)

## 2. Trigger and workload

- No queue reads and no start parameters in the source; the trigger is external to the export (scheduler or manual run).

## 3. Inputs

| Input | Description | Source | Evidence |
|---|---|---|---|
| Disputes | Collection of dispute records to be loaded, each with a dispute reference, account number, dispute amount, and transaction date. | Pre-populated data item (collection) within the process, presumably sourced upstream from a case management or transaction system | For Each Dispute (loop_start) |

## 4. Outputs

| Output | Description | Destination | Evidence |
|---|---|---|---|
| Q-Disputes queue items | Each dispute record is added as an item to the Q-Disputes work queue for downstream processing. | Q-Disputes work queue | Add Dispute To Queue (queue_write) |
| ItemCount | Total count of dispute items successfully loaded into the queue during this run, returned as a process output. | Process output (End stage) | End (end); Count Loaded (calculation) |

## 5. Current-state flow

Stages in link order from Start, true branches first; stages
reachable only through exception recovery follow at the end.

| # | Stage | Type | Detail | Reached |
|---|---|---|---|---|
| 1 | Start | start |  |  |
| 2 | For Each Dispute | loop_start | for each row in `Disputes` |  |
| 3 | Add Dispute To Queue | queue_write | Add To Queue on Q-Disputes |  |
| 4 | Stage Dsipute Row | calculation | 4 assignment(s) |  |
| 5 | Count Loaded | calculation | `[ItemCount] + 1` |  |
| 6 | For Each Dispute | loop_end |  |  |
| 7 | End | end |  |  |

## 6. Business rules

**BR-1.** Every dispute in the input collection must be added to the Q-Disputes work queue for downstream handling.

Evidence: For Each Dispute (loop_start); Add Dispute To Queue (queue_write)

**BR-2.** Each queue item must carry the dispute reference, account number, dispute amount, and transaction date.

Evidence: Stage Dsipute Row (calculation)

**BR-3.** The process must report the number of dispute items loaded into the queue as an output.

Evidence: Count Loaded (calculation); End (end)

## 7. Current exception handling

None identified in the reviewed spec.

## 8. Systems and dependencies

- Queue **Q-Disputes**: 0 read(s), 1 add(s), 0 disposition(s)

## 9. Human touchpoints

None identified in the reviewed spec.

## 10. Configuration

No configuration constants in the source.
